/**
 * An implementation of a checker function that checks if 2 STL Strings are
 * permutations of each other or not.
 * Assumptions: Using only lowercase letters
 *			   Spaces are ignored
 * @author Deepak Kakad
 */

#pragma once
#include <stdio.h>
#include <string>

using namespace std;
bool isPerm(string c1, string c2);
