/**
 * An implementation of a checker function that checks if a 2 strings are just
 * one edit away from each other or not. An edit is defined as insertion, deletion 
 * or a replacement.
 * @author Deepak Kakad
 */

#pragma once
#include <stdio.h>

bool isOneAway(char a[], char b[]);