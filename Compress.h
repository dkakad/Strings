/**
* An implementation of a string manipulator that performs basic string
* compression. In this implementation, a compressed string is represented
* by the character followed by it's frequency in the original string.
* @author Deepak Kakad
*/

#pragma once
#include <stdio.h>
#include <string>

using namespace std;

string Compress(char c[]);